PiCalculator Example
====================

This example calculates the value of Pi to the specified number of digits. It demonstrates compiling a GThread-derived class into a separate .dll. Only this .dll is added to the grid application's manifest. 

.\PiCalculator contains the GThread-derived class.

.\PiCalculatorExec contains the executable.

 